import { Routes } from '@angular/router';

export const routes: Routes = [
  // Currently using a single-page layout, so no specific routes are needed.
  // You can add routes here later, e.g.:
  // { path: 'tasks', component: TaskListComponent },
];
